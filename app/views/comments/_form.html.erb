<%= form_for @comment do |f| %>
	<p>
		<%= f.label :name %></br>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :body %></br>
		<%= f.text_area :body %>
	</p>
	<p>
		<%= f.label :person_id %></br>
		<%= f.number_field :person_id %>
	</p>
	
	<p>
		<%= f.submit %>
	</p>
<% end %>